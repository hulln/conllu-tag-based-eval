# archive/

Historical outputs kept for traceability.

- Not the canonical source for current reporting.
- Use `results/runs/` and `predictions/runs/` for active canonical outputs.
- Run snapshots moved from active paths live under `archive/runs/<run-id>/`.

This folder preserves older experiment layouts and artifacts.

Current archived run snapshot:

- `archive/runs/20260414-1215_sl-ssj-ud-test_full/` (metadata-preserving verification run)
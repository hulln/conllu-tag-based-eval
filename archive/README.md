# archive/

Historical outputs kept for traceability.

- This folder is not part of the public reporting surface.
- Active public artifacts are in `predictions/output/`, `results/output/`, and `tables/`.
- Local private run snapshots should be stored under `archive/local/`.
